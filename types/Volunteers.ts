export type Volunteer = {
    firstName:string
    group: {groupId: string, groupName: string}
    id: string
    joinedAt: string
    lastName: string
    meetingCount: string
    numOfMeetings: string
    performance: number
    phone: string
    age: string
    _id:string
}